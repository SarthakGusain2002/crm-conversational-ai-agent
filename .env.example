ANTHROPIC_API_KEY=

# Optional: only needed if you want to connect to a real CRM OData sandbox
# instead of the built-in mock mode. Never commit real values.
CRM_TENANT_URL=
CRM_USERNAME=
CRM_PASSWORD=
